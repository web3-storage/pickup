{
  "name": "ipfs-pinning-service-lambda",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "openapi-types": "npx openapi-typescript https://raw.githubusercontent.com/ipfs/pinning-services-api-spec/main/ipfs-pinning-service.yaml --output schema.ts"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.2.12",
    "@serverless-stack/resources": "^1.2.12",
    "@tsconfig/node16": "^1.0.2",
    "standard": "^17.0.0",
    "typescript": "^4.7.2",
    "vitest": "^0.13.0"
  },
  "workspaces": [
    "backend"
  ]
}
